{
    "tasks": [
        {
            "type": "shell",
            "label": "go run active file",
            "command": "go",
            "args": [
                "run",
                "${file}"
            ],
            "options": {
                "cwd": "${fileDirname}"
            },
            "group": {
                "kind": "test",
                "isDefault": true
            }
        }
    ],
    "version": "2.0.0"
}